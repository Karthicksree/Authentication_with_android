# Use the README file as a reference to customize settings.

Comment:
  fields:
    default: body, user_id, post_id
    form: body, user_id, post_id
  relationships: mediums
  application: Application
