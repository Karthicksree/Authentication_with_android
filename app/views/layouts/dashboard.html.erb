<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Authen App" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Authen App" %>">
    <%= stylesheet_link_tag 'dashboard', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'dashboard', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="row">
      <header>
        <%= render 'layouts/navigation' %>
      </header>

      <div class="col s12 m4 l3" id="sidebar">

        <%= render 'layouts/user_profile' %>
      </div>

      <div class="col s12 m8 l9" id="content_right">
        <main role="main">
           <%= render 'layouts/messages' %>
           <%= yield %>
        </main>
      </div>

    </div>
    
    
  </body>
</html>
